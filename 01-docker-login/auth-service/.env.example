# Copia este archivo a .env y ajusta si es necesario.
# En docker-compose estas variables ya se inyectan automaticamente;
# este archivo sirve para correr el auth-service fuera de Docker.

DB_HOST=mariadb
DB_PORT=3306
DB_NAME=dispositivos_medicos
DB_USER=root
DB_PASSWORD=root123

PORT=5001

# Firma de los JWT. Cambialo por algo secreto fuera de desarrollo.
JWT_SECRET=dev-secret-ce5508-cambialo
JWT_EXPIRA_MIN=60
