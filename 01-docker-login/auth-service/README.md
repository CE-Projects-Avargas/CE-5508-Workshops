# auth-service — el reto de hoy

Esta carpeta está vacía a propósito.

El reto: construyan un contenedor nuevo, separado del backend de `/proyectos`, que hable con la misma base de datos MariaDB y resuelva el login — registrar un usuario y autenticarlo.

Cómo lo resuelvan es su decisión.
